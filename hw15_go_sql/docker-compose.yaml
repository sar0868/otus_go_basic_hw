version: "3"

services:
  pg:
    image: postgres
    environment:
      POSTGRES_DB: shop
      POSTGRES_USER: username
      POSTGRES_PASSWORD: passwd
      PGDATA: /var/lib/postgresql/data/shop
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data/shop

volumes:
  db-data:
